import React from "react"

const Button = () => {
    return (
        <button className="w-full sm:w-max sm:px-28 py-4 bg-blue-button font-semibold text-white rounded-full">
            Apply now
        </button>
    )
}

export default Button
